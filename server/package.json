{
  "name": "server",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "watch": "npx tsx watch index.ts",
    "start": "bun run --hot index.ts",
    "prisma-init": "npx prisma generate; npx prisma migrate reset --force; npx prisma migrate dev --name init"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.3.3",
    "@prisma/client": "^6.9.0",
    "bcryptjs": "^3.0.2",
    "dotenv": "^16.5.0",
    "elysia": "^1.3.4",
    "jsonwebtoken": "^9.0.2",
    "prisma": "^6.9.0",
    "uuid": "^11.1.0"
  }
}
